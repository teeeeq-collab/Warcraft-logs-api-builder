# Keep Windows batch files with CRLF endings: cmd.exe can mishandle `goto`
# labels and multi-line blocks in an LF-only .bat file.
*.bat text eol=crlf

# Everything else stays LF in the repository.
*.py text eol=lf
*.md text eol=lf
*.yml text eol=lf
*.toml text eol=lf
*.graphql text eol=lf
